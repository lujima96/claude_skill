# Blender Reports

Phase 7 sample reports live here until a real `.blend` source asset exists.

These samples show the report shape expected from `blender_scripts/` and are intentionally marked as sample data. Real reports should be generated by Blender with commands from `blender_scripts/README.md`.

